<nav class="navbar-default navbar-static-side" role="navigation">
    <div class="sidebar-collapse">
        <ul class="nav" id="side-menu">
            <li class="nav-header">
                <div class="dropdown profile-element" >
                    <a data-toggle="dropdown" class="dropdown-toggle" href="#">
                        <span>
                            @*<img alt="image" class="img" height="70" width="70" src="#">*@
                        </span>

                        <span class="clear">
                            <span class="block m-t-xs">
                                <strong class="font-bold">@User.Identity.Name</strong>
                            </span>
                        </span>
                    </a>
                    <ul class="dropdown-menu animated fadeInRight m-t-xs">
                        <li><a href="/Identity/Account/Manage">My Profile</a></li>
                    </ul>
                </div>
                <div class="logo-element">
                    +
                </div>
            </li>
            @if (User.IsInRole("BDO"))
            {
                <li class="@*@Html.IsSelected(action: " Index")*@">
                    <a href="@Url.Action("Index", "Home")"><i class="fa fa-laptop"></i> <span class="nav-label">Go Home</span> </a>
                </li>
                <li class="@*@Html.IsSelected(action: " Index")*@">
                    <a href="@Url.Action("Index", "Profiles")"><i class="fa fa-laptop"></i> <span class="nav-label">Add Beneficiaries</span> </a>
                </li>
                <li class="@*@Html.IsSelected(action: " Minor")*@">
                    <a href="@Url.Action("Beneficiaries", "Activity")"><i class="fa fa-desktop"></i> <span class="nav-label">Add Activity</span></a>
                </li>
            }
            @if (User.IsInRole("Administrator"))
            {
                <li class="@*@Html.IsSelected(action: " Index")*@">
                    <a href="@Url.Action("Index", "Home")"><i class="fa fa-laptop"></i> <span class="nav-label">Go Home</span> </a>
                </li>
                <li class="@*@Html.IsSelected(action: " Index")*@">
                    <a href="@Url.Action("Register", "Account")"><i class="fa fa-laptop"></i> <span class="nav-label">Add Users</span> </a>
                </li>
            }

        </ul>
    </div>
</nav>